<?xml version = "1.0" encoding = "UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD com.example.Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace = "com.cmic.attendance.dao.GroupAuditDao">
    <sql id="groupAuditColumns">
        a.id AS "id",
        a.audit_phone AS "auditPhone",
        a.audit_name AS "auditName",
        a.enterprise_id AS "enterpriseId",
        a.enterprise_name AS "enterpriseName",
        a.daily_rule_id AS "dailyRuleId",
        a.remarks AS "remarks",
        a.create_by AS "createBy.id",
        a.create_date AS "createDate",
        a.update_by AS "updateBy.id",
        a.update_date AS "updateDate"
    </sql>

    <sql id="groupAuditJoins">
    </sql>

    <select id="get" resultType="GroupAudit">
        SELECT
        <include refid="groupAuditColumns"/>
        FROM t_group_audit a
        <include refid="groupAuditJoins"/>
        WHERE a.id = #{id}
    </select>

    <select id="findList" resultType="GroupAudit">
        SELECT
        <include refid="groupAuditColumns"/>
        FROM t_group_audit a
        <include refid="groupAuditJoins"/>
        <where>
            1 = 1
        </where>
    </select>

    <select id="findAllList" resultType="GroupAudit">
        SELECT
        <include refid="groupAuditColumns"/>
        FROM t_group_audit a
        <include refid="groupAuditJoins"/>
    </select>

    <insert id="insert">
        INSERT INTO t_group_audit(
            id,
            audit_phone,
            audit_name,
            enterprise_id,
            enterprise_name,
            daily_rule_id,
            remarks,
            create_by,
            create_date,
            update_by,
            update_date
        ) VALUES (
            #{id},
            #{auditPhone},
            #{auditName},
            #{enterpriseId},
            #{enterpriseName},
            #{dailyRuleId},
            #{remarks},
            #{createBy.id},
            #{createDate},
            #{updateBy.id},
            #{updateDate}
        )
    </insert>

    <update id="update">
        UPDATE t_group_audit SET
        audit_phone = #{auditPhone},
        audit_name = #{auditName},
        enterprise_id = #{enterpriseId},
        enterprise_name = #{enterpriseName},
        daily_rule_id = #{dailyRuleId},
        remarks = #{remarks},
        create_by = #{createBy.id},
        update_by = #{updateBy.id},
        update_date = #{updateDate}
        WHERE id = #{id}
    </update>

    <update id="dynamicUpdate">
        UPDATE t_group_audit SET
        <if test="auditPhone != null  and auditPhone != ''">
        audit_phone = #{auditPhone},
        </if>
        <if test="auditName != null  and auditName != ''">
        audit_name = #{auditName},
        </if>
        <if test="enterpriseId != null  and enterpriseId != ''">
        enterprise_id = #{enterpriseId},
        </if>
        <if test="enterpriseName != null  and enterpriseName != ''">
        enterprise_name = #{enterpriseName},
        </if>
        <if test="dailyRuleId != null  and dailyRuleId != ''">
        daily_rule_id = #{dailyRuleId},
        </if>
        update_by = #{updateBy.id},
        update_date = #{updateDate}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM t_group_audit
        WHERE id = #{id}
    </delete>

    <select id="getGroupAuditBydrId" resultType="GroupAudit">
        SELECT
        <include refid="groupAuditColumns"/>
        FROM t_group_audit a
        <include refid="groupAuditJoins"/>
        WHERE a.daily_rule_id = #{dailyRuleId}
    </select>
</mapper>