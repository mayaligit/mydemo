<?xml version = "1.0" encoding = "UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD com.example.Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace = "com.cmic.attendance.dao.PermissionDao">
    <sql id="permissionColumns">
        a.id AS "id",
        a.permission_name AS "permissionName",
        a.parent_id AS "parentId",
        a.group_menu AS "groupMenu",
        a.url AS "url",
        a.status AS "status",
        a.pcode AS "pcode",
        a.create_date AS "createDate",
        a.update_date AS "updateDate",
        a.create_by AS "createBy.id",
        a.update_by AS "updateBy.id",
        a.remarks AS "remarks"
    </sql>

    <sql id="permissionJoins">
    </sql>

    <select id="get" resultType="Permission">
        SELECT
        <include refid="permissionColumns"/>
        FROM t_permission a
        <include refid="permissionJoins"/>
        WHERE a.id = #{id}
    </select>

    <select id="findList" resultType="Permission">
        SELECT
        <include refid="permissionColumns"/>
        FROM t_permission a
        <include refid="permissionJoins"/>
        <where>
            1 = 1
        </where>
    </select>

    <select id="findAllList" resultType="Permission">
        SELECT
        <include refid="permissionColumns"/>
        FROM t_permission a
        <include refid="permissionJoins"/>
    </select>

    <insert id="insert">
        INSERT INTO t_permission(
            id,
            permission_name,
            parent_id,
            group_menu,
            url,
            status,
            pcode,
            create_date,
            update_date,
            create_by,
            update_by,
            remarks
        ) VALUES (
            #{id},
            #{permissionName},
            #{parentId},
            #{groupMenu},
            #{url},
            #{status},
            #{pcode},
            #{createDate},
            #{updateDate},
            #{createBy.id},
            #{updateBy.id},
            #{remarks}
        )
    </insert>

    <update id="update">
        UPDATE t_permission SET
        permission_name = #{permissionName},
        parent_id = #{parentId},
        group_menu = #{groupMenu},
        url = #{url},
        status = #{status},
        pcode = #{pcode},
        update_date = #{updateDate},
        update_by = #{updateBy.id},
        remarks = #{remarks}
        WHERE id = #{id}
    </update>

    <update id="dynamicUpdate">
        UPDATE t_permission SET
        <if test="permissionName != null  and permissionName != ''">
        permission_name = #{permissionName},
        </if>
        <if test="parentId != null  and parentId != ''">
        parent_id = #{parentId},
        </if>
        <if test="groupMenu != null ">
        group_menu = #{groupMenu},
        </if>
        <if test="url != null  and url != ''">
        url = #{url},
        </if>
        <if test="status != null ">
        status = #{status},
        </if>
        <if test="pcode != null  and pcode != ''">
        pcode = #{pcode},
        </if>
        update_by = #{updateBy.id},
        update_date = #{updateDate}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM t_permission
        WHERE id = #{id}
    </delete>
</mapper>